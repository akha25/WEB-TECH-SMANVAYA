"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.tsx
var index_exports = {};
__export(index_exports, {
  TICKER_DIRECTION_LEFT: () => TICKER_DIRECTION_LEFT,
  TICKER_DIRECTION_RIGHT: () => TICKER_DIRECTION_RIGHT,
  default: () => index_default
});
module.exports = __toCommonJS(index_exports);
var import_react = __toESM(require("react"));
var import_framer_motion = require("framer-motion");
var TICKER_DIRECTION_LEFT = -1;
var TICKER_DIRECTION_RIGHT = 1;
var noop = () => {
};
var Ticker = (props) => {
  const {
    children,
    duration = 10,
    direction = "left",
    isPlaying = true,
    pauseOnHover = false,
    gap = 0,
    onMouseEnter = noop,
    onMouseLeave = noop
  } = props;
  const [tickerEl, setTickerEl] = import_react.default.useState(null);
  const [tickerContentsEl, setTickerContentsEl] = import_react.default.useState(null);
  const [tickerContentWidth, setTickerContentWidth] = import_react.default.useState(2);
  const [numDupes, setNumDupes] = import_react.default.useState(1);
  const [isHovered, setIsHovered] = import_react.default.useState(false);
  const [scope, animate] = (0, import_framer_motion.useAnimate)();
  const [animationControls, setAnimationControls] = import_react.default.useState(void 0);
  const isInView = (0, import_framer_motion.useInView)(scope);
  const [prefersReducedMotion, setPrefersReducedMotion] = import_react.default.useState(false);
  const childArray = import_react.default.Children.toArray(children);
  const directionMultiplier = direction === "left" || direction === TICKER_DIRECTION_LEFT ? TICKER_DIRECTION_LEFT : TICKER_DIRECTION_RIGHT;
  import_react.default.useEffect(() => {
    const mql = window.matchMedia("(prefers-reduced-motion: reduce)");
    setPrefersReducedMotion(mql.matches);
    const handler = (e) => setPrefersReducedMotion(e.matches);
    mql.addEventListener("change", handler);
    return () => mql.removeEventListener("change", handler);
  }, []);
  import_react.default.useEffect(() => {
    if (tickerEl && tickerContentsEl) {
      const measure = () => {
        const contentWidth = tickerContentsEl.getBoundingClientRect().width;
        if (contentWidth === 0) return;
        setTickerContentWidth(contentWidth);
        setNumDupes(Math.max(Math.ceil(2 * tickerEl.clientWidth / contentWidth), 1));
      };
      measure();
      const observer = new ResizeObserver(measure);
      observer.observe(tickerEl);
      observer.observe(tickerContentsEl);
      return () => observer.disconnect();
    }
  }, [tickerEl, tickerContentsEl]);
  import_react.default.useEffect(() => {
    if (isInView && tickerContentWidth > 2) {
      if (animationControls) {
        animationControls.stop();
      }
      const controls = animate(
        scope.current,
        { x: tickerContentWidth * directionMultiplier },
        { ease: "linear", duration, repeat: Infinity }
      );
      if (!isPlaying || prefersReducedMotion) {
        controls.pause();
      }
      setAnimationControls(controls);
    }
  }, [isInView, tickerContentWidth, directionMultiplier, duration]);
  import_react.default.useEffect(() => {
    if (animationControls) {
      if (!isInView || !isPlaying || pauseOnHover && isHovered || prefersReducedMotion) {
        animationControls.pause();
      } else {
        animationControls.play();
      }
    }
  }, [isInView, isPlaying, isHovered, pauseOnHover, prefersReducedMotion]);
  const handleMouseEnter = () => {
    setIsHovered(true);
    onMouseEnter();
  };
  const handleMouseLeave = () => {
    setIsHovered(false);
    onMouseLeave();
  };
  return /* @__PURE__ */ import_react.default.createElement(
    "div",
    {
      className: "FMT__container",
      ref: setTickerEl,
      style: {
        width: "100%",
        height: "100%",
        overflow: "hidden"
      },
      onMouseEnter: handleMouseEnter,
      onMouseLeave: handleMouseLeave
    },
    /* @__PURE__ */ import_react.default.createElement(
      "div",
      {
        ref: scope,
        className: "FMT__container__contents",
        style: {
          display: "flex",
          width: "max-content",
          gap: gap + "px",
          marginLeft: `-${tickerContentWidth * numDupes}px`
        }
      },
      /* @__PURE__ */ import_react.default.createElement("div", { style: { display: "flex", gap: gap + "px" } }, [...Array(numDupes)].map(
        (_) => childArray.map((item, index) => /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, { key: index }, item))
      )),
      /* @__PURE__ */ import_react.default.createElement("div", { ref: setTickerContentsEl, style: { display: "flex", gap: gap + "px" } }, childArray.map((item, index) => /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, { key: index }, item))),
      /* @__PURE__ */ import_react.default.createElement("div", { style: { display: "flex", gap: gap + "px" } }, [...Array(numDupes)].map(
        (_) => childArray.map((item, index) => /* @__PURE__ */ import_react.default.createElement(import_react.default.Fragment, { key: index }, item))
      ))
    )
  );
};
var index_default = Ticker;
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  TICKER_DIRECTION_LEFT,
  TICKER_DIRECTION_RIGHT
});
